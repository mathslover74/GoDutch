<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <%- include ('../partials/head.ejs') -%>
  </head>
  <body>
    <%- include ('../partials/nav2.ejs') -%>
    
    <h1><%= expenses.name; %></h1>
  <h2>Bank Amount Balance</h2>
  <button><a href="/expenses/new">Create Expenses</a></button>
  <table class="u-full-width">
    <thead>
      <tr>
        <th>Date</th>
        <th>Friend</th>
        <th>Description</th>
        <th>Paid by</th>
        <th>Amount ($)</th>
        <th>Delete</th>
      </tr>
    </thead>
    <tbody>      
         <tr>
           <td><%= expenses.date %></td>
           <td><%= expenses.name %></td>
           <td><a href="/expenses/<%= expenses.id%>"><%= expenses.description; %></a></td>
           <!-- <td><a href="/<%= expenses.id%>"><%= expenses.description; %></a></td> -->
           <% if (expenses.paidByYou === true) { %>
            <td>true</td>
          <% } else { %>
            <td>false</td>
          <% } %>
            <td><%= expenses.amount/2 %></td>
            <td>
              <form action="/expenses/<%=expenses.id; %>?_method=DELETE" method="POST">
              <input type="submit" value="DELETE" />
            </form>
          </td>
           
           
          </tr>
    </tbody>
  </table>

  <!-- <div class='showbox'>
    <h4><%= expenses.name; %></h4>
    <h4>$<%= expenses.description; %></h4> -->
    
    <!--  ADD EDIT HERE-->
    <br><a href="/expenses/dashboard">Back to Dashboard</a>
</div>
  </body>
</html>